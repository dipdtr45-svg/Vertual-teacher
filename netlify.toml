[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"
  # Set GEMINI_API_KEY in Netlify site settings (do NOT commit secrets)
  GEMINI_API_KEY = "${GEMINI_API_KEY}"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
